# Reverse-a-Number-Using-Python-While-Loop
This Python program takes a number as input from the user and reverses its digits using a while loop. The program extracts each digit using the modulus operator (%), builds the reversed number by multiplying and adding digits, and finally prints the reversed result. This example demonstrates the use of loops, integer division.
